//azure_create_vpc.go
package main
import (
  "fmt"
)

func azure_create_vpc(region string, environment string) {
  fmt.Println("Creating Azure vpc in region: " + region + " for environment: " + environment)
}
