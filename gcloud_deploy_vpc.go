//gcloud_deploy_vpc.go
package main
import (
  "fmt"
)

func gcloud_deploy_vpc(region string, environment string) {
  fmt.Println("Deploying GCloud vpc in region: " + region + " for environment: " + environment)
}
