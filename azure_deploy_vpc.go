//azure_deploy_vpc.go
package main
import (
  "fmt"
)

func azure_deploy_vpc(region string, environment string) {
  fmt.Println("Deploying Azure vpc in region: " + region + " for environment: " + environment)
}
