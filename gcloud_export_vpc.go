//gcloud_export_vpc.go
package main
import (
  "fmt"
)

func gcloud_export_vpc(region string, environment string) {
  fmt.Println("Exporting GCloud vpc in region: " + region + " for environment: " + environment)
}
