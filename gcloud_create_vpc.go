//gcloud_create_vpc.go
package main
import (
  "fmt"
)

func gcloud_create_vpc(region string, environment string) {
  fmt.Println("Creating GCloud vpc in region: " + region + " for environment: " + environment)
}
