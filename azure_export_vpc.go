//azure_export_vpc.go
package main
import (
  "fmt"
)

func azure_export_vpc(region string, environment string) {
  fmt.Println("Exporting Azure vpc in region: " + region + " for environment: " + environment)
}
