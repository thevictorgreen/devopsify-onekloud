//gcloud_update_vpc.go
package main
import (
  "fmt"
)

func gcloud_update_vpc(region string, environment string) {
  fmt.Println("Updating GCloud vpc in region: " + region + " for environment: " + environment)
}
